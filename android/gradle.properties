org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# 项目在 D 盘、pub 缓存插件源在 C 盘，跨盘符导致 Kotlin 增量缓存相对路径计算失败（different roots）。
# 关闭增量编译可规避 RelocatableFileToPathConverter 报错。
kotlin.incremental=false
# 全局统一 JVM 目标为 17，与本机 JDK（Java 17）对齐。
# 各模块 Java 编译目标已在根 build.gradle.kts 的 subprojects 中强制为 17，
# 与 Kotlin 对齐，避免 "Inconsistent JVM Target Compatibility" 导致 BUILD FAILED。
android.kotlin.jvmTarget=17
