# 更新日志

本文件记录 EverLink 所有值得关注的变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。分类固定使用：`新增` / `变更` /
`修复` / `移除` / `废弃` / `安全`。

**发布流程**：平时把变更写进下方 `[未发布]` 段落；发版时将其改写为
`## [x.y.z] - YYYY-MM-DD`，同步 `pubspec.yaml` 的 `version` 字段，再打 `vx.y.z`
标签推送。CI 会用 `scripts/extract_changelog.py` 抽出该版本段落作为 GitHub Release
的说明正文，GitHub 自动生成的提交列表会追加在其后。

> 该说明同时展示在应用内的「发现新版本」弹窗中，因此请写面向终端用户的中文描述，
> 不要直接粘贴 commit 标题。

## [1.2.5] - 2026-08-31

### 变更

- 底部菜单栏改为无文字的毛玻璃胶囊样式：去掉图标下方的文字标签，选中项由一颗平滑滑动的圆形高亮表示，长按图标可查看名称
- 关闭「菜单栏悬浮」时的内嵌底栏同样隐藏文字并压低高度，两种样式保持一致
- 加宽并强化胶囊菜单栏的视觉效果：增大图标尺寸与间距、提高玻璃不透明度使菜单从背景中更清晰分离；选中高亮增强填充浓度与投影、未选中图标更哑，层次对比更明显
- 快传网页端验证过频道 PIN 后会记住凭据，再次打开同一频道无需重复输入；若频道 PIN 被修改或频道已解散，会自动清除记录并重新提示输入

### 修复

- 频道 PIN 输入框在窄屏手机上不再超出弹窗，六个方框会按可用宽度自动收缩并保持正方形
- 修复「加入 / 创建频道」里点击「随机生成」PIN 导致应用崩溃的问题
- 修复悬浮胶囊菜单栏：改用叠层布局稳定贴底居中，宽度按内容自适应避免窄约束下横向溢出；底部用 viewPadding 兜底系统手势条，避免被底部指示条遮挡
- 修复应用内下载更新：安装包下载完成后进度弹框仍一直显示"下载中"的问题（下载完成事件被原生抢前台的安装器打断而丢失）；现改为下载完成显示「下载完成」并交由「立即安装」按钮拉起安装

## [1.2.4] - 2026-08-30

### 新增

- 更新检查会按当前运行的平台挑选安装包：Android 取 `.apk`、Windows 取 `.exe`、
  Linux 取 `.AppImage`/`.deb`/`.snap`、macOS 取 `.dmg`、iOS 取 `.ipa`
- Windows 端可直接在应用内下载安装包，下载完成后自动拉起安装程序
- 更新弹窗显示本次将要下载的安装包文件名

### 变更

- 更新提示改为响应式面板：手机端从底部弹出，桌面端居中显示，并新增下载进度卡片
- 下载过程中禁止误触关闭弹窗
- 版本号统一以 `pubspec.yaml` 为唯一来源，构建时校验标签与其一致

### 修复

- Windows 用户不再被错误推送 Android 的 APK 更新包
- iOS 安装包在弹窗中明确标注「未签名」，提示需自行重签后才能安装到真机

### 移除

- 删除已失效的 `update.json` 配置文件，更新检查统一走 GitHub Releases 接口

## [1.2.3] - 2026-08-30

### 修复

- Windows 安装包内置简体中文安装向导语言文件，修复安装程序无法构建的问题

## [1.2.2] - 2026-08-30

### 修复

- 修正 Windows 安装程序的图标引用路径，恢复安装包构建

## [1.2.1] - 2026-08-30

### 修复

- 重制 Windows 应用图标为标准多尺寸格式，修复 Windows 版本构建失败

## [1.2.0] - 2026-08-30

### 新增

- 新增 Windows 桌面版，提供安装包与免安装便携版两种形式
- 新增 iOS 未签名安装包构建（需自行重签名后安装）
- 各平台安装包统一命名为 `EverLink-<版本>-<平台>[-<架构>].<扩展名>`
